# Pointer API base the extension talks to (the /api/ext/* facade lives here).
# Dev: your local pointer-ton (e.g. http://localhost:3001). Prod: https://pointer.trade
VITE_POINTER_API_BASE=https://pointer.trade
