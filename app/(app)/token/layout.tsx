/** Stable segment for `/token/*` — dock overlays and layout prefs persist across mint changes. */
export default function TokenSegmentLayout({ children }: { children: React.ReactNode }) {
  return children;
}
